# Agent Change

**Request:** help me change the heading on the homepage — specifically: Hero heading

**Proposed changes:**

Here's what needs to change and how to do it:

---

*What's happening*

The hero heading is stored in `content/strings.json` under `hero.headline`. You just need to update that value — no component changes needed.

---

*The change*

In `content/strings.json`, find this line:

```json
"headline": "Your favourite house",
```

Update it to whatever the new heading should be. For example:

```json
"headline": "Homes that tell a story",
```

---

*Full updated `hero` block for reference*

```json
"hero": {
  "headline": "Homes that tell a story",
  "subheadline": "Handcrafted pieces made by artisans across India — for the home you're building, one intentional object at a time.",
  "cta_primary": "Shop Collections",
  "cta_secondary": "Our Story",
  "badge": "Festive Collection Now Live"
},
```

---

*That's the only file you need to touch* — `content/strings.json`. The component reading `strings.hero.headline` will pick it up automatically.

Let me know what you'd like the heading to say and I'll give you the exact copy to drop in! ✏️
