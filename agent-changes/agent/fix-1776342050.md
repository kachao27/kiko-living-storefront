# Agent Change

**Request:** change the add to cart button color to green

**Proposed changes:**

Looking at the codebase, the Add to Cart button lives in `components/ProductCard.js` (and likely `pages/product/[id].js` based on the PDP strings). I can see the current brand uses `bg-[#C4702B]` (burnt orange) for primary actions.

Here's what needs to change:

---

*Changes needed:*

The Add to Cart button styling needs to be updated from the current orange (`#C4702B`) to green. I'll use `#1B4332` (the dark green already in your brand palette, used for the "NEW" badge) as the primary, with a slightly lighter hover state.

*File: `components/ProductCard.js`*

Find the Add to Cart button at the bottom of the card (truncated in the context but it follows the price section):

```jsx
// Before
<button className="... bg-[#C4702B] hover:bg-[#A85D22] ...">
  {strings.pdp.add_to_cart_button}
</button>

// After
<button className="... bg-[#1B4332] hover:bg-[#2D6A4F] ...">
  {strings.pdp.add_to_cart_button}
</button>
```

*File: `pages/product/[id].js`* — same swap on the PDP Add to Bag button.

---

*Exact classes to replace across both files:*

| Location | Before | After |
|---|---|---|
| ProductCard button bg | `bg-[#C4702B]` | `bg-[#1B4332]` |
| ProductCard button hover | `hover:bg-[#A85D22]` | `hover:bg-[#2D6A4F]` |
| PDP button bg | `bg-[#C4702B]` | `bg-[#1B4332]` |
| PDP button hover | `hover:bg-[#A85D22]` | `hover:bg-[#2D6A4F]` |

> ⚠️ *Note:* This change only affects the *Add to Cart/Bag button*. Other orange elements (collection label text, discount badge, bestseller badge, nav hover states) are intentionally left unchanged since those aren't buttons.

---

Want me to open a PR with these changes? I'll need the full content of `components/ProductCard.js` and `pages/product/[id].js` to make the exact edits — the codebase context above is truncated. Drop those files and I'll have the PR ready.
