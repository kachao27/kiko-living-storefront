# Agent Change

**Request:** help me change the heading — specifically: Copy change

**Proposed changes:**

The request is a bit open-ended — "change the heading" with a copy change could mean several things. Based on the codebase, the most likely candidate is the *hero headline* in `content/strings.json`, which currently reads:

`"headline": "Your favourite house"`

This feels incomplete — it's likely meant to be something more evocative. Here's what I'd suggest, but *please confirm the exact copy you want* before I make the change.

---

*Proposed change in `content/strings.json`:*

```json
"hero": {
  "headline": "Beautiful homes, made by hand.",
  ...
}
```

---

*If that's the right field*, the only file you need to edit is:

*`content/strings.json`* — find this line:

```json
"headline": "Your favourite house",
```

And replace it with your desired copy, for example:

```json
"headline": "Beautiful homes, made by hand.",
```

---

*Could you clarify:*

1. *Which heading* — hero headline, catalog page title (`"Our Collection"`), nav brand name, or something else?
2. *What the new copy should be* — exact wording helps avoid a follow-up round

All heading copy lives in `content/strings.json`, so once you confirm the field and new text, it's a one-line change.
